# supabase/tests/ — Disposable integration notes (Phase 1 scaffolding)

Future home for SQL/HTTP fixtures that run only against an authorized disposable project.

Phase 1 executable tests live under repo `tests/security/` (pure contracts, offline).

Do not point these at production.
