# supabase/tests/fixtures/ — Synthetic disposable fixtures (placeholder)

Future disposable-only users/tasks/objects. Use synthetic UUIDs only.

Do not store production JWTs, keys, or project refs here.
