# BabyDragon / NetField-360 — agent memory

Before any F10C2 / CR1 / database / APK / dashboard work, read **`docs/f10c2/CONTINUITY.md`**.

Hard rules:

- Never discard, reset, clean, stash, or revert the dirty working tree (CR1-B through CR1-E).
- Do not stage, commit, push, PR, or deploy unless the owner explicitly asks.
- Do not execute SQL 214 (quarantined `supabase/drafts/f10c2/never-run/214/`), reapply 215, reapply 216, or run 009 / 010 / 012 / 013 / 112 / 207.
- Do not contact production (prefix `nsne`).
- Do not create or connect to a permanent database until the owner authorizes a named staging project.
- Never print credentials, tokens, or database URLs. Use environment-variable **names** only.
- Protected queue `bd-rf-1787606300946` must never be uploaded. Do not click Sync Now unless authorized.
- Synthetic open task `F10C2-P4BU-E2E` is known disposable residue (unproven prior assignment). Do not restore or SQL-alter the assignment row.
- **CR1-D-R2 E2E Data Rule** is Inactive on disposable after authorized 216 + Admin UI Deactivate. Effective fallback for `F10C2-P4BU-E2E` is **CR1-B disposable default**. Do not SQL-reactivate it.
